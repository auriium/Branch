package me.aurium.beetle.branch.fallback.message.impl;

import me.aurium.beetle.branch.handlers.context.NodeContext;
/*

public class DefaultBuilderMessageProvider implements BuilderMessageProvider<Object> {

    @Override
    public void handleDefaultSingle(NodeContext<Object> context) {


    }

    @Override
    public void handleDefaultBranching(NodeContext<Object> context) {

        */
/*StringBuilder fuk = new StringBuilder("/");

        context.fullPath().withoutTop().getAllBlocks().getBackingList().forEach(blok -> {
            fuk.append(blok).append(" ");
        });

        String finalFuk = fuk.toString();

        List<String> alternatives = new ArrayList<>(); //TODO make parameter

        for (String string : alternatives) {
            context.messageSender(finalFuk + string);
        }*//*



    }

    @Override
    public void handleDefaultValue(NodeContext<Object> context) {

    }
}
*/
