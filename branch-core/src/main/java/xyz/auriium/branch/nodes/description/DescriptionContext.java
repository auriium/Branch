package xyz.auriium.branch.nodes.description;

/**
 * everything a description needs (this node name, etc etc etc etc)
 *
 * Calls to node.getDescription should return a **STRING**, this object should not be known by the api at large and instead
 * should be considered an object used in order to act as a transport for description message orders.
 */
public class DescriptionContext {
}
